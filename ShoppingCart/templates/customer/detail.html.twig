{% block body %}
   <div class="container col-md-5 text-center mb-3">
      <div class="row">
         {# <div class="col">
            <img src="{{ employee.image }}" width="300" height="400" />
         </div> #}
         <div class="col">
            <h1>Email: {{ customer.email }}</h1>
            <h2>Name: {{ customer.name }} $</h2>
            <h2>PhoneNumber: {{ customer.phoneNumber}}</h2>
         </div>
      </div>
   </div>
{% endblock %}